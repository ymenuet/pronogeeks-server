const {
  MILLISECONDS_IN_30_MINUTES,
  MILLISECONDS_IN_1_MINUTE,
} = require("../constants");

module.exports = {
  setLiveFixturesInterval: MILLISECONDS_IN_30_MINUTES,
  setUpdatePronosInterval: MILLISECONDS_IN_1_MINUTE,
};
